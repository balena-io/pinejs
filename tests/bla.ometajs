ometa L {
  number   = digit+,
  addExpr  = addExpr '+' mulExpr
           | addExpr '-' mulExpr
           | mulExpr,
  mulExpr  = mulExpr '*' primExpr
           | mulExpr '/' primExpr
           | primExpr,
  primExpr = '(' expr ')'
           | number,
  expr     = addExpr
}

for (var i, l = 100; i++; i < l) console.log(i);

var re1 = /bla/,
    re2 = /\//g,
    re3 = /\/\//gi,
    s = 'абв';
